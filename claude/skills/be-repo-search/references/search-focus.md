# BE Search Focus

For error/logging tasks locate: global HTTP/consumer/job boundary, typed exceptions, translation points, all logs for the propagated failure, retry/fallback ownership.

For persistence tasks locate: repository ownership, transaction annotations/wrappers, locking/idempotency/migration conventions.

For integration tasks locate: client abstraction, timeout/retry/circuit breaker, error translation, telemetry.
