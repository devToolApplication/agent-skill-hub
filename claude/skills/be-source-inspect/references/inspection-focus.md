# BE Inspection Focus

Normalize exception behavior as `source -> catch? -> translate? -> log? -> rethrow/return? -> final boundary`.

Normalize persistence behavior as `transaction owner -> repository calls -> external calls -> commit/rollback risk`.

Report evidence, not conclusions.
