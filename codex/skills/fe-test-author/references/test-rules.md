# FE Test Rules

Prefer tests mapped to stable requirement/acceptance IDs.

Examples:

- translated error uses semantic error code mapping;
- pluralized text changes with count;
- theme preference persists through established abstraction;
- mobile presentation exposes required actions without duplicate fetch;
- dialog focus/keyboard behavior satisfies UIUX requirement;
- mutation invalidates the expected query.
